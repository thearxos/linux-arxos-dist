# linux-arxos-dist
Delivers the ArxOS kernel's over-the-air runtime payload. `install.sh` applies the workload
tuning (sysctl, udev, limits) and any live patches without a reboot. Kernel packages ship
separately: R2 carries the current build, GitHub releases carry the history for rollback.
