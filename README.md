# linux-arxos-dist — runtime delivery mirror (public)
Applies the ARXOS kernel's over-the-air runtime payload: `install.sh` installs the workload
tuning (sysctl/udev/limits) and any live patches, no reboot. Kernel packages ship separately
(R2 = latest, GitHub releases = history). The build recipe is private.
